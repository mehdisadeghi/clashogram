apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: warlog
  namespace: clashogram
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi